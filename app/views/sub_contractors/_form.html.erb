<%= form_with(model: sub_contractor, local: true) do |form|%>
  <% if sub_contractor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_contractor.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% sub_contractor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </p>

  <%= form.submit 'Create Subcontractor' %>
<% end %>